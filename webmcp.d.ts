// @mcp-b/webmcp-types ships declarations only — no runtime entry point — so it
// is referenced rather than imported. This is what augments `Document` with
// `modelContext`.
/// <reference types="@mcp-b/webmcp-types" />
